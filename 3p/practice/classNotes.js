export class Node {
    constructor(value) {
      //`value` almacena el valor del nodo (puede ser cualquier tipo de dato, como números o cadenas).
      this.value = value;
      //Hijito izquierdo inicializado como nadota.
      this.left = null;
      //Hijito derecho inicializado como nadota.
      this.right = null;
    }
  }
  